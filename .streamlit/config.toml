# Streamlit Community Cloud applies its own server binding; keep this file
# limited to portable options. For local Docker, pass flags instead, e.g.:
#   streamlit run app.py --server.address 0.0.0.0 --server.port 8501

[browser]
gatherUsageStats = false

[server]
maxUploadSize = 200
